# Copy to `.env` and add your key. `.env` is gitignored — it is never committed.
#   cp .env.example .env
# Only the `ask` command (LLM generation) needs this. Indexing, retrieval, and
# retrieval evaluation all run without any API key.
ANTHROPIC_API_KEY=<add-api-key-here>